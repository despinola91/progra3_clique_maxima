package vista;
import controlador.ControladorGrafo;

public class MainView {
	private ControladorGrafo controller;
	// Aca inicializan sus pantallas, definen el controlador, definen metodos para que el controlador ejecute lo que ustedes digan ejemplo mostrarCliqueMaxima
	
	/*
	 * public void mostrarClique(List<Vertice> clique) {
        StringBuilder sb = new StringBuilder("Clique máxima encontrada:\n");
        for (Vertice v : clique) {
            sb.append(v.getId()).append(" (").append(v.getPeso()).append(")\n");
        }
        JOptionPane.showMessageDialog(this, sb.toString());
    }
	 * 
	 * 
	 * 
	 * 
	 * */
}
